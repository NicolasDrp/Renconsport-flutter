name: renconsport_flutter
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  adaptive_theme: ^3.4.0
  appinio_swiper: ^2.0.3
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.0.0
  google_fonts: ^5.1.0
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.24.3

flutter:
  uses-material-design: true
  assets:
    - assets/
